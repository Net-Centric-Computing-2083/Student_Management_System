@{
    ViewData["Title"] = "Profile Setup Required";
}
<div class="container mt-4">
    <div class="alert alert-warning">
        <h4>Student profile not found</h4>
        <p>Your login account is not currently linked to a student profile.</p>
        <p>Please ask an administrator or teacher to create your student record using the same email address, then sign in again.</p>
    </div>
    <form asp-controller="Account" asp-action="Logout" method="post">
        <button class="btn btn-secondary">Logout</button>
    </form>
</div>
